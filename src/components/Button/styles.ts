import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    marginTop: 8,
    marginBottom: 8,
    borderRadius: 5,
    padding: 16,
    justifyContent: "center",
    alignItems: "center",
  },
  buttonPrimary: {
    backgroundColor: "#067789",
  },
  textPrimary: {
    color: "#fff",
  },
  buttonSecondary: {
    backgroundColor: "#fff",
    borderWidth: 2,
    borderColor: "#067789",
  },
  textSecondary: {
    color: "#067789",
  },
});
